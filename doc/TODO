try a simple performance test to compare speed of or comparison vs map vs array comparison
add has next with comparator or list of characters to character reader

add unit tests
    for parsers
    for tokenizer
    for readers

spaces and comments should be removed in code preprocessor or code parser
