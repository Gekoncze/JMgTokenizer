add unit tests
    for parsers
        double quote
        name
        number
        single quote
        special
        whitespace

    for tokenizer

spaces and comments should be removed in code preprocessor or code parser
